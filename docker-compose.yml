name: survivor-deprecated

# Deprecated: this project no longer uses Docker Compose for deployment.
# Use deploy-fullstack.cmd instead. The current deployment model is:
# - host Nginx serves /survivor and proxies /survivor/api
# - Docker runs only the backend container with docker start/restart
services: {}
