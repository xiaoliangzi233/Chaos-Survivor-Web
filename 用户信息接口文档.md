# 用户信息接口文档

赛数助理提供用户信息查询接口，外部应用可通过以下方式获取当前登录用户的基本信息。

---

## 方式二：直接调用 HTTP 接口（所有语言通用）

SDK 底层就是 HTTP 请求，其他语言或非 Spring 项目可直接调用。

### 接口信息

| 项目 | 说明 |
|---|---|
| 请求方式 | GET |
| 接口地址 | `http://113.249.91.32/sszl/user/simple-info` |
| 请求头 | `Authorization: <token>` |

### 返回数据

```json
{
    "id": "a1b2c3d4...",
    "username": "张三",
    "employeeId": "000001"
}
```

---

## 获取 token

在赛数助理管理后台配置外部应用后，用户点击该应用跳转时，会在 URL 上自动拼接 token 参数，无需手动处理。

**跳转 URL 格式：**

```
http://外部应用地址/?token=xxx
```

外部应用从 URL 参数中获取 token 后，通过上述任一方式即可查询当前用户信息。

---

## 返回字段说明

| 字段 | 类型 | 说明 |
|---|---|---|
| id | String | 用户 ID |
| username | String | 用户姓名 |
| employeeId | String | 员工工号 |

---

## 常见问题


```

**Q：调用返回 null 或 401？**

A：检查两点：
1. `sszl.client.base-url` 是否配置正确（非 Java 项目检查请求地址）
2. token 是否有效（是否从同一后端地址获取的 token）

**Q：更新 SDK 版本后本地还是旧的？**

A：执行时加 `-U` 参数强制刷新：

```bash
mvn clean compile -U
```
